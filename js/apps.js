function sum(num1, num2) {
    let result = (num1 + num2) * 5;
    return result;
}
console.log(sum(4,8));

function users(user1, user2, user3, user4, user5, user6, user7, user8, use9, user10, user11) {
    if (users > 10) {
        return `true`;
    } else {
        return `false`;
    }
}


const element = `photons`;
console.log(element.startsWith(`ph`));
console.log(element.startsWith(`ph, 2`));
